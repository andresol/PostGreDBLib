package info.sollie.db.mssql;
public enum Syntax {
    INT,
    DOUBLE,
    REPLACE,
    INSERT,
    SELECT,
    UPDATE,
    INTO,
    SET,
    VALUES,
    FROM,
    WHERE,
    ORDER,
    BY,
    HAVING,
    ID,
    SCOPE_IDENTITY,
    AS, 
    DELETE, DESC, INNER, JOIN, ON, IN;

}
